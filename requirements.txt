# ============================================================
# requirements.txt
# 课程视频智能剪辑系统 — 依赖项
#
# 安装说明（按顺序执行）:
#
# 第一步：安装 CPU 版 PyTorch（必须单独安装，不在此文件中）
#   pip install torch==2.2.2+cpu torchaudio==2.2.2+cpu --index-url https://download.pytorch.org/whl/cpu
#
# 第二步：安装本文件中的其他依赖
#   pip install -r requirements.txt
# ============================================================

# ---------- 核心科学计算 ----------
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4

# ---------- 机器学习 ----------
scikit-learn==1.4.2

# ---------- 视频 / 图像处理 ----------
opencv-python==4.9.0.80
Pillow==12.2.0

# ---------- 音频处理 ----------
soundfile==0.12.1
librosa==0.10.2
pydub==0.25.1
audioread==3.0.1

# ---------- 语音识别（CPU 友好）----------
faster-whisper==1.0.3
ctranslate2==4.3.1

# ---------- OCR（自带模型，无需额外安装）----------
easyocr==1.7.1

# ---------- 中文 NLP ----------
jieba==0.42.1
pypinyin==0.51.0

# ---------- 进度条 ----------
tqdm==4.66.4

# ---------- 工具 ----------
# 注意：torch / torchaudio 请按上方说明单独安装 CPU 版本
